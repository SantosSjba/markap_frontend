/**
 * Form helpers — VeeValidate + Yup (schemas definidos en cada vista/módulo).
 */
export { useForm, Field, ErrorMessage, Form } from 'vee-validate'
export { toTypedSchema } from '@vee-validate/yup'
